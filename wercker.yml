---

services:
  - mongo:3.2

box: java:8-jdk
dev:
  steps:
    - script:
        name: show environment variables
        code: env
    - script:
        name: run gradle
        code: ./gradlew run

