import be.pixxis.zoufzouf.ProjectVersion

ext.buildTimestamp = new Date().format('yyyy-MM-dd HH:mm:ss')

def build = System.env.CI_BUILD_NUMBER // Codeship
if (build == null) {
    build = System.env.SOURCE_BUILD_NUMBER
}
project.version = new ProjectVersion(System.env.MAJOR_VERSION, System.env.MINOR_VERSION, build)